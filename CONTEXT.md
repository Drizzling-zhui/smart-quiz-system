# 智能题库系统 - 项目上下文（供AI继续开发用）

## 项目位置
`C:\Users\Administrator\Desktop\quiz-app\index.html`

单文件应用（HTML + CSS + JavaScript），浏览器直接打开即可运行。

## 当前状态
已完成基础框架，但文本解析（正则版本）仍有BUG，推荐使用AI解析替代。

## 技术栈
纯前端，单页应用。无框架依赖。数据存储使用 `localStorage`。

## 功能清单

### 已实现
1. **学科管理** — 侧边栏增删改学科，每学科独立题目库
2. **题库浏览** — 按题型筛选（单选/填空/简答），显示每题的答题统计（尝试次数/错误次数/正确率）
3. **答题模式** — 两种模式：顺序答题 / 随机答题。每题提交后显示对错反馈，记录答题统计
4. **文本解析导入** — 正则版（有BUG，不完善），AI版（调用外部API）
5. **JSON导入** — 带自动修复功能（去除尾逗号、补全引号等8层容错）
6. **JSON导出** — 一键导出备份
7. **API设置** — 支持配置任意 OpenAI 兼容接口（地址/模型/密钥），未配置时弹出引导公告
8. **选择题答案可切换** — 提交前可自由切换选项（已修复）

### 数据模型（JSON结构）
```json
{
  "subjects": [{
    "name": "学科名",
    "description": "",
    "questions": [{
      "id": 1234567890,
      "type": "choice|fill|short",
      "question": "题干",
      "options": [{"label":"A","text":"选项"}],
      "answer": "A",
      "score": 1.3,
      "explanation": "解析",
      "stats": {"attempts": 5, "correct": 3, "wrong": 2}
    }]
  }]
}
```

## 已知问题

### BUG 1：正则解析器不完善
**文件位置**：`parseQuizText()` 函数（约第1086行）
**症状**：输入 "A.\nUDP\n\nB.\nIP" 这种选项字母和文本分行的格式时，选项文本为空字符串，实际文本被加到题干中。
**根因**：旧正则 `^([A-Za-z])[.、．]\s*(.*)` 的 `(.*)` 匹配空字符串，把空文本选项推进数组。
**当前修复**：改为 `(.+)` 并要求至少一个字符，新增 `optAlone` 分支处理单独一行的选项字母。
**但仍存在的问题**：答案提取正则用分号做分隔符，如果答案文本本身包含分号就出错：
```
我的答案:C:server; client;正确答案:C:server; client;
```
应该改用 `正确答案：` 作为锚点切分，而不是分号。

### BUG 2：数据迁移不完善
`loadData()` 中的迁移只处理了"选项文本为空"的情况，对更多脏数据形态（答案没提取到等）修复不够健壮。

## 推荐的改进方向

1. **弃用正则解析**：AI解析已能处理任意格式，正则解析可以降级为辅助
2. **支持更多题型**：多选题、判断题、匹配题等
3. **UI美化**：更好的响应式设计、动画效果
4. **数据导出格式**：支持导出为 PDF/Word
5. **批量操作**：批量删除、移动题目
6. **PWA支持**：离线可用
7. **用户系统**：多用户、云端同步

## 关键函数索引
- `loadData()` / `saveData()` — 数据持久化（约第477行）
- `parseQuizText(text)` — 正则文本解析（约第1086行）
- `aiParseText()` — AI文本解析（约第1199行）
- `fixJSON(input)` — JSON自动修复（约第1176行）
- `selectQuizOption(label)` — 选择题选项交互（约第919行）
- `submitQuizAnswer()` — 提交答案（约第942行）
- `renderBrowse()` — 浏览模式渲染（约第527行）
- `renderQuizQuestion()` — 答题模式渲染（约第822行）
- `startQuiz()` — 开始答题（约第791行）
- `getApiConfig()` / `saveApiConfig()` — API配置（约第519行）

## 样式关键变量
CSS变量定义在 `:root` 中，色系以 `#4f6ef7` 为主色调。
